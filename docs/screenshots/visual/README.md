# Visual verification artifacts

These PNGs are generated by the visual Playwright target using deterministic API fixtures. Each state is captured at 1366×768, 1440×900, and 1920×1080. The screenshot test asserts the expected UI state before capture; the real-stack Playwright test separately verifies the migrated API, worker, fixture source, and frontend workflow.
